# Fotos no Cloudflare R2 (armazenamento trocável) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Passar o armazenamento das fotos de recebimento para o Cloudflare R2, atrás de uma porta trocável, com o Supabase Storage (+ export/limpeza) escondido como plano B.

**Architecture:** Uma porta `ArmazenamentoFotos` (domínio) com dois adapters — R2 (ativo, SDK S3) e Supabase (dormente, embrulha o Storage atual). Uma env `FOTOS_STORAGE` escolhe o adapter e, no servidor, controla a visibilidade da tela de export/limpeza. O `anexo-repository` passa a delegar as 3 operações de storage para a porta; nada de metadados/banco muda.

**Tech Stack:** Next.js 16 (Server Components/Actions), TypeScript strict (`noUncheckedIndexedAccess`), Supabase, Cloudflare R2 via `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`, vitest.

## Global Constraints

- **AGENTS.md:** "This is NOT the Next.js you know — read `node_modules/next/dist/docs` before writing Next code." Next 16.
- **Subagentes NÃO instalam credenciais reais nem dão `git push`.** O smoke com R2 real depende do usuário criar o bucket e pôr as env; `tsc`/`lint`/`build` **não** precisam das credenciais.
- **Porta trocável:** o app fala só com `ArmazenamentoFotos`. R2 e S3 da AWS compartilham o adapter (S3-compatível). Não portar o export/limpeza (é o plano B, fica Supabase direto).
- **Uma env `FOTOS_STORAGE` = `r2` (padrão) | `supabase`** controla o adapter ativo E a visibilidade do export/limpeza (visível só em `supabase`). A env **não é** `NEXT_PUBLIC` → o gate do menu é resolvido no **servidor** (layout) e passado como prop pro `AppShell` (client).
- **Sem duplicar foto, sem migrar as de teste, sem migração de banco.** Chave continua UUID (`${processoId}/${uuid}.${ext}`) — **não renomear** no upload.
- **Mantém:** comprimir 1 MB no cliente, máx 3 fotos, exclusão remove storage + metadado, rollback de objeto órfão, exibição por URL assinada curta (1 h).
- **Env R2 (server-only):** `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`.
- TS strict `noUncheckedIndexedAccess`. Repositório usa `createServerSupabase`. Trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Verificação:** `npx tsc --noEmit` + `npm run lint` + `npm run build`; `npm run test` (TDD no domínio). SEM push.

## File Structure

- **Create** `src/modules/recebimento/domain/armazenamento-fotos.ts` — porta + `resolverModoStorage`.
- **Create** `src/modules/recebimento/domain/__tests__/armazenamento-fotos.test.ts` — TDD.
- **Create** `src/modules/recebimento/infra/armazenamento/r2.ts` — adapter R2.
- **Create** `src/modules/recebimento/infra/armazenamento/supabase.ts` — adapter Supabase (dormente).
- **Create** `src/modules/recebimento/infra/armazenamento/index.ts` — `armazenamentoAtual()` + `modoStorageFotos()`.
- **Modify** `src/modules/recebimento/infra/anexo-repository.ts` — as 3 funções de storage delegam à porta.
- **Modify** `src/shared/ui/app-shell.tsx` — prop `exportarFotosVisivel`, filtra o item.
- **Modify** `src/app/(app)/layout.tsx` — passa `exportarFotosVisivel` via `modoStorageFotos()`.
- **Modify** `src/app/(app)/recebimento/exportar-fotos/page.tsx` — guard "indisponível" fora do modo supabase.
- **Modify** `.env.example` (se existir) — documenta as env novas.

---

### Task 1: Domínio — porta + resolverModoStorage (TDD)

**Files:**
- Create: `src/modules/recebimento/domain/armazenamento-fotos.ts`
- Create: `src/modules/recebimento/domain/__tests__/armazenamento-fotos.test.ts`

**Interfaces:**
- Produces:
  - `type ModoStorage = 'r2' | 'supabase'`
  - `interface ArmazenamentoFotos { subir(chave: string, dados: ArrayBuffer, mime: string): Promise<void>; urlAssinada(chave: string, segundos?: number): Promise<string>; remover(chave: string): Promise<void> }`
  - `resolverModoStorage(valor: string | undefined): ModoStorage`

- [ ] **Step 1: Escrever os testes (que falham)**

Criar `src/modules/recebimento/domain/__tests__/armazenamento-fotos.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { resolverModoStorage } from '../armazenamento-fotos'

describe('resolverModoStorage', () => {
  it("'r2' → r2", () => expect(resolverModoStorage('r2')).toBe('r2'))
  it("'supabase' → supabase", () => expect(resolverModoStorage('supabase')).toBe('supabase'))
  it('undefined → r2 (default)', () => expect(resolverModoStorage(undefined)).toBe('r2'))
  it('vazio → r2', () => expect(resolverModoStorage('')).toBe('r2'))
  it('desconhecido → r2', () => expect(resolverModoStorage('drive')).toBe('r2'))
  it('trim + case-insensitive', () => expect(resolverModoStorage('  SUPABASE ')).toBe('supabase'))
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm run test -- armazenamento-fotos`
Expected: FAIL (módulo `../armazenamento-fotos` não existe).

- [ ] **Step 3: Implementar**

Criar `src/modules/recebimento/domain/armazenamento-fotos.ts`:

```ts
export type ModoStorage = 'r2' | 'supabase'

/**
 * Contrato de armazenamento de fotos. O app depende só desta porta; o backend
 * concreto (R2 ativo, Supabase dormente, futuramente S3/Drive) fica atrás dela.
 * A `chave` é o caminho do objeto (hoje `${processoId}/${uuid}.${ext}`).
 */
export interface ArmazenamentoFotos {
  subir(chave: string, dados: ArrayBuffer, mime: string): Promise<void>
  /** URL assinada de curta duração para exibir/baixar (padrão 1 h). */
  urlAssinada(chave: string, segundos?: number): Promise<string>
  remover(chave: string): Promise<void>
}

/** Resolve o modo a partir do valor de env. Default 'r2'; só 'supabase' (após
 *  trim/lowercase) escolhe o plano B; qualquer outra coisa cai em 'r2'. */
export function resolverModoStorage(valor: string | undefined): ModoStorage {
  return valor?.trim().toLowerCase() === 'supabase' ? 'supabase' : 'r2'
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm run test -- armazenamento-fotos`
Expected: PASS (6 casos).

- [ ] **Step 5: Verificar tipos e commit**

Run: `npx tsc --noEmit`
Expected: sem erros.

```bash
git add src/modules/recebimento/domain/armazenamento-fotos.ts src/modules/recebimento/domain/__tests__/armazenamento-fotos.test.ts
git commit -m "feat(fotos): porta ArmazenamentoFotos + resolverModoStorage (TDD)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Dependência + adapters (R2 e Supabase) + factory

**Files:**
- Modify: `package.json` (via `npm install`)
- Create: `src/modules/recebimento/infra/armazenamento/r2.ts`
- Create: `src/modules/recebimento/infra/armazenamento/supabase.ts`
- Create: `src/modules/recebimento/infra/armazenamento/index.ts`
- Modify: `.env.example` (se existir)

**Interfaces:**
- Consumes: `ArmazenamentoFotos`, `ModoStorage`, `resolverModoStorage` (Task 1); `createServerSupabase` de `@/shared/lib/supabase/server`.
- Produces:
  - `criarArmazenamentoR2(): ArmazenamentoFotos` (`./r2`)
  - `criarArmazenamentoSupabase(): ArmazenamentoFotos` (`./supabase`)
  - `armazenamentoAtual(): ArmazenamentoFotos` e `modoStorageFotos(): ModoStorage` (`./index`)

- [ ] **Step 1: Instalar o SDK S3**

Run: `npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner`
Expected: adiciona as duas dependências ao `package.json` e ao lockfile.

- [ ] **Step 2: Adapter R2**

Criar `src/modules/recebimento/infra/armazenamento/r2.ts`:

```ts
import {
  DeleteObjectCommand,
  GetObjectCommand,
  PutObjectCommand,
  S3Client,
} from '@aws-sdk/client-s3'
import { getSignedUrl } from '@aws-sdk/s3-request-presigner'
import type { ArmazenamentoFotos } from '../../domain/armazenamento-fotos'

/** Lê uma env obrigatória do R2 ou lança um erro claro (evita erro críptico do SDK). */
function env(nome: string): string {
  const valor = process.env[nome]
  if (!valor) throw new Error(`Configuração do R2 ausente: ${nome}.`)
  return valor
}

/** Adapter Cloudflare R2 (S3-compatível). Bucket privado; exibição por URL assinada. */
export function criarArmazenamentoR2(): ArmazenamentoFotos {
  const bucket = env('R2_BUCKET')
  const client = new S3Client({
    region: 'auto',
    endpoint: `https://${env('R2_ACCOUNT_ID')}.r2.cloudflarestorage.com`,
    credentials: {
      accessKeyId: env('R2_ACCESS_KEY_ID'),
      secretAccessKey: env('R2_SECRET_ACCESS_KEY'),
    },
  })

  return {
    async subir(chave, dados, mime) {
      await client.send(
        new PutObjectCommand({
          Bucket: bucket,
          Key: chave,
          Body: new Uint8Array(dados),
          ContentType: mime,
        }),
      )
    },
    async urlAssinada(chave, segundos = 3600) {
      return getSignedUrl(client, new GetObjectCommand({ Bucket: bucket, Key: chave }), {
        expiresIn: segundos,
      })
    },
    async remover(chave) {
      await client.send(new DeleteObjectCommand({ Bucket: bucket, Key: chave }))
    },
  }
}
```

- [ ] **Step 3: Adapter Supabase (dormente)**

Criar `src/modules/recebimento/infra/armazenamento/supabase.ts` (embrulha exatamente o que o `anexo-repository` fazia):

```ts
import { createServerSupabase } from '@/shared/lib/supabase/server'
import type { ArmazenamentoFotos } from '../../domain/armazenamento-fotos'

const BUCKET = 'anexos-processos'

/** Adapter Supabase Storage — o plano B. Mantém o comportamento atual. */
export function criarArmazenamentoSupabase(): ArmazenamentoFotos {
  return {
    async subir(chave, dados, mime) {
      const supabase = await createServerSupabase()
      const { error } = await supabase.storage
        .from(BUCKET)
        .upload(chave, dados, { contentType: mime, upsert: false })
      if (error) throw error
    },
    async urlAssinada(chave, segundos = 3600) {
      const supabase = await createServerSupabase()
      const { data, error } = await supabase.storage.from(BUCKET).createSignedUrl(chave, segundos)
      if (error || !data) throw error ?? new Error('Falha ao gerar URL do anexo.')
      return data.signedUrl
    },
    async remover(chave) {
      const supabase = await createServerSupabase()
      const { error } = await supabase.storage.from(BUCKET).remove([chave])
      if (error) throw error
    },
  }
}
```

- [ ] **Step 4: Factory + modo**

Criar `src/modules/recebimento/infra/armazenamento/index.ts`:

```ts
import type { ArmazenamentoFotos, ModoStorage } from '../../domain/armazenamento-fotos'
import { resolverModoStorage } from '../../domain/armazenamento-fotos'
import { criarArmazenamentoR2 } from './r2'
import { criarArmazenamentoSupabase } from './supabase'

/** Modo de storage ativo (lido da env FOTOS_STORAGE; default 'r2'). */
export function modoStorageFotos(): ModoStorage {
  return resolverModoStorage(process.env.FOTOS_STORAGE)
}

let cache: { modo: ModoStorage; impl: ArmazenamentoFotos } | null = null

/** Adapter de armazenamento ativo. Memoizado por modo. */
export function armazenamentoAtual(): ArmazenamentoFotos {
  const modo = modoStorageFotos()
  if (cache && cache.modo === modo) return cache.impl
  const impl = modo === 'supabase' ? criarArmazenamentoSupabase() : criarArmazenamentoR2()
  cache = { modo, impl }
  return impl
}
```

- [ ] **Step 5: Documentar as env**

Se existir `.env.example` na raiz, acrescentar (senão pular este step):

```
# Armazenamento de fotos: 'r2' (padrão) ou 'supabase' (plano B)
FOTOS_STORAGE=r2
# Credenciais do Cloudflare R2 (bucket privado) — server-only
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
```

- [ ] **Step 6: Verificar tipos/build e commit**

Run: `npx tsc --noEmit && npm run build`
Expected: sem erros (não precisa das credenciais reais para compilar).

```bash
git add package.json package-lock.json src/modules/recebimento/infra/armazenamento/ .env.example
git commit -m "feat(fotos): adapters R2 e Supabase atrás da porta + factory por env

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Ligar o anexo-repository à porta

**Files:**
- Modify: `src/modules/recebimento/infra/anexo-repository.ts`

**Interfaces:**
- Consumes: `armazenamentoAtual` de `./armazenamento`.
- Produces: `subirObjeto`/`gerarUrlAnexo`/`removerObjeto` com a mesma assinatura de hoje, agora delegando à porta. (`listarAnexosComUrl`, metadados e tipos **não mudam**.)

- [ ] **Step 1: Delegar as 3 funções de storage**

Em `src/modules/recebimento/infra/anexo-repository.ts`:

1. Adicionar o import no topo (após o import de `createServerSupabase`):

```ts
import { armazenamentoAtual } from './armazenamento'
```

2. **Remover** a constante `const BUCKET = 'anexos-processos'` (linha 3) — ela só era usada pelas 3 funções de storage, que agora delegam. (Os metadados usam `createServerSupabase`, que continua.)

3. Substituir as 3 funções pelo delegate (mantendo os JSDoc):

```ts
/** Signed URL (1 h) para exibir/baixar um objeto do storage ativo. */
export async function gerarUrlAnexo(path: string): Promise<string> {
  return armazenamentoAtual().urlAssinada(path, 3600)
}

/** Sobe um objeto para o storage ativo (falha se o path já existir, no adapter). */
export async function subirObjeto(path: string, dados: ArrayBuffer, mime: string): Promise<void> {
  await armazenamentoAtual().subir(path, dados, mime)
}

/** Remove um objeto do storage ativo. */
export async function removerObjeto(path: string): Promise<void> {
  await armazenamentoAtual().remover(path)
}
```

(A `listarAnexosComUrl` continua chamando `gerarUrlAnexo` e já trata falha por foto — inalterada.)

- [ ] **Step 2: Verificar tipos/lint/build e commit**

Run: `npx tsc --noEmit && npm run lint && npm run build`
Expected: sem erros (confirmar que `BUCKET` não ficou sem uso e que `createServerSupabase` ainda é usado pelas funções de metadados).

```bash
git add src/modules/recebimento/infra/anexo-repository.ts
git commit -m "feat(fotos): anexo-repository delega storage para a porta (R2 ativo)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Esconder export/limpeza fora do modo Supabase

**Files:**
- Modify: `src/shared/ui/app-shell.tsx`
- Modify: `src/app/(app)/layout.tsx`
- Modify: `src/app/(app)/recebimento/exportar-fotos/page.tsx`

**Interfaces:**
- Consumes: `modoStorageFotos` de `@/modules/recebimento/infra/armazenamento`.

- [ ] **Step 1: `AppShell` aceita e aplica `exportarFotosVisivel`**

Em `src/shared/ui/app-shell.tsx`:

1. Adicionar `exportarFotosVisivel` à assinatura (props):

```tsx
export function AppShell({
  nome,
  email,
  perfilNome,
  permissoes,
  exportarFotosVisivel,
  children,
}: {
  nome: string
  email: string
  perfilNome: string
  permissoes: Perms
  exportarFotosVisivel: boolean
  children: React.ReactNode
}) {
```

2. Excluir o item `exportar-fotos` do menu quando não visível. Trocar a linha:

```tsx
  const recebimentoVisivel = RECEBIMENTO.filter((i) => pode(i.perm))
```

por:

```tsx
  const recebimentoVisivel = RECEBIMENTO.filter(
    (i) => pode(i.perm) && (i.chave !== 'exportar-fotos' || exportarFotosVisivel),
  )
```

- [ ] **Step 2: `layout.tsx` resolve o modo no servidor e passa a prop**

Em `src/app/(app)/layout.tsx`:

1. Adicionar o import:

```tsx
import { modoStorageFotos } from '@/modules/recebimento/infra/armazenamento'
```

2. Passar a prop no `<AppShell>` (junto de `permissoes`):

```tsx
    <AppShell
      nome={sessao.nome}
      email={sessao.email}
      perfilNome={sessao.perfil.nome}
      permissoes={sessao.perfil.permissoes}
      exportarFotosVisivel={modoStorageFotos() === 'supabase'}
    >
```

(Se algum nome de prop acima — `email` etc. — divergir do arquivo real, mantenha os existentes e apenas **acrescente** `exportarFotosVisivel`.)

- [ ] **Step 3: Guard na página de exportar-fotos**

Em `src/app/(app)/recebimento/exportar-fotos/page.tsx`, adicionar o import e o guard logo após a checagem de permissão:

```tsx
import { modoStorageFotos } from '@/modules/recebimento/infra/armazenamento'
```

```tsx
  if (!sessao || !podeFazer(sessao.perfil, 'administrar')) {
    return <SemPermissao descricao="Você não tem permissão para exportar fotos." />
  }
  if (modoStorageFotos() !== 'supabase') {
    return (
      <SemPermissao descricao="A exportação de fotos está indisponível no modo de armazenamento atual." />
    )
  }
```

- [ ] **Step 4: Verificar e commit**

Run: `npx tsc --noEmit && npm run lint && npm run build`
Expected: sem erros. (Confirmar que o `AppShell` client NÃO importa `modoStorageFotos` — a prop vem do layout server; o aws-sdk não deve entrar no bundle do cliente.)

```bash
git add "src/shared/ui/app-shell.tsx" "src/app/(app)/layout.tsx" "src/app/(app)/recebimento/exportar-fotos/page.tsx"
git commit -m "feat(fotos): esconde export/limpeza fora do modo supabase (plano B)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Verificação final

**Files:** nenhum (só verificação).

- [ ] **Step 1: Suite completo**

Run: `npx tsc --noEmit && npm run lint && npm run build && npm run test`
Expected: tudo verde; os testes de `armazenamento-fotos` entre eles. O único warning aceitável é o pré-existente de `<img>` em `anexos-processo.tsx`.

- [ ] **Step 2: Smoke (anotar; NÃO fazer push)**

Pré-requisito do smoke com R2 real: o usuário cria o bucket R2 privado + token com escopo e põe `R2_*` + `FOTOS_STORAGE=r2` no `.env.local`. (Sem isso, o modo R2 falha no upload — esperado.)

- **Modo R2** (`FOTOS_STORAGE=r2`): abrir um processo → **Adicionar foto** → sobe no R2 e **aparece no card "Fotos (N/3)"** do processo; excluir → some; o menu **não** mostra "Exportar Fotos"; acessar `/recebimento/exportar-fotos` por link → "indisponível".
- **Plano B** (`FOTOS_STORAGE=supabase`, reiniciar o dev): anexar → vai pro Supabase; "Exportar Fotos" **reaparece** no menu; export/limpeza funcionam como hoje.

- [ ] **Step 3: NÃO fazer push**

Commits ficam locais; o usuário valida o smoke e decide.

---

## Notas de verificação (self-review do plano)

**Cobertura da spec:**
- Porta + adapters R2/Supabase + factory por env → Tasks 1–2. ✅
- R2 ativo, bucket privado, URL assinada → Task 2 (r2.ts). ✅
- `anexo-repository` delega; metadados intocados → Task 3. ✅
- Env controla adapter E visibilidade do export → Tasks 2 (factory) + 4 (gates). ✅
- Export/limpeza não portado (plano B) → não tocamos `anexo-export-repository.ts`. ✅
- Sem migração de banco, sem duplicar, chave UUID, mantém 1 MB/3/rollback → Global Constraints + Task 3 (assinaturas preservadas; `anexos-actions` intocado). ✅
- Gate resolvido no servidor (env não pública) → Task 4 (layout server passa prop pro AppShell client). ✅

**Consistência de tipos:** `ArmazenamentoFotos`/`ModoStorage`/`resolverModoStorage` (Task 1) usados em 2; `armazenamentoAtual`/`modoStorageFotos` (Task 2) usados em 3 e 4. `subir/urlAssinada/remover` batem com o uso em `subirObjeto/gerarUrlAnexo/removerObjeto`. ✅

**Sem placeholders:** todo passo de código traz o código completo. O único condicional ("se `.env.example` existir") tem ação definida (pular). ✅
