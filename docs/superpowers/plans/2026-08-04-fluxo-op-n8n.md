# Fluxo da OP estilo n8n — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Uma aba "Fluxo" em Análise que mostra o fluxo de postos de uma OP num canvas estilo n8n (React Flow), com o WIP por posto (peças ali agora), Manutenção como ramo, e detalhe por nó (aprovadas/reprovadas/retestes/SNs).

**Architecture:** RPC `sf_fluxo_op` agrega os números no Postgres (WIP = último bipe; reprova → Manutenção). Um repositório junta os agregados com a ordem do fluxo (`sf_ordem_postos`) e o mapa posto→perfil (temStatus). Um builder puro monta nós/arestas. O client renderiza com `@xyflow/react`; a lista de SNs de cada posto é carregada sob demanda ao abrir o nó.

**Tech Stack:** Next.js 16 (App Router, Server Actions), React 19, TypeScript strict, Tailwind v4, Supabase (Postgres/RLS + RPC security-definer), `@xyflow/react` (novo), Vitest 4.

## Global Constraints
- **PT-BR** em UI, comentários e mensagens. Commit trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Só leitura** — não edita o fluxo da OP. O olhinho da lista de OPs (`ordens/fluxo-botao.tsx`) fica intacto.
- **Migração aplicada no Dev primeiro** (a promoção pro Prod é fora deste plano, no batch merge). Dev/Prod hoje em **0070**; esta é a **0071**.
- RPC `security definer` com guard `tem_permissao('visualizar')` (padrão do módulo).
- **WIP (posição atual):** por SN, o registro mais recente (`data_hora desc, created_at desc`); `status='reprovado'` (case-insensitive) → conta em **`'Manutenção'`**; senão → conta no `posto` do registro. SN nunca bipado não conta.
- **Agregados** (aprovadas/reprovadas/registros/retestes) são sobre **todos** os registros do posto. **Retestes** = SNs distintos com **≥2 registros** no mesmo posto.
- **Build + lint + testes verdes** ao fim de cada task. Gerenciador de pacotes: **npm**.
- Postos casam por nome **case-insensitive** (convenção do `dashboard.ts`).

## File Structure
- **Create** `supabase/migrations/0071_sf_fluxo_op.sql` — RPC de agregados.
- **Create** `src/modules/shopfloor/domain/fluxo-op.ts` — tipos + builder puro de nós/arestas.
- **Create** `src/modules/shopfloor/domain/__tests__/fluxo-op.test.ts` — testes do builder.
- **Create** `src/modules/shopfloor/infra/fluxo-repository.ts` — `carregarFluxoOp`, `carregarSnsDoPosto`, `listarOrdens`.
- **Create** `src/modules/shopfloor/application/fluxo-actions.ts` — actions com guard.
- **Create** `src/app/(app)/shopfloor/analisar/fluxo/page.tsx` — server (lista de OPs).
- **Create** `src/app/(app)/shopfloor/analisar/fluxo/fluxo-form.tsx` — client (seletor + canvas).
- **Create** `src/app/(app)/shopfloor/analisar/fluxo/fluxo-node.tsx` — nó custom do React Flow.
- **Modify** `src/app/(app)/shopfloor/analisar/layout.tsx` — acrescenta a aba "Fluxo".
- **Modify** `package.json` / `package-lock.json` — dependência `@xyflow/react`.

---

## Task 1: Migração 0071 — RPC `sf_fluxo_op`

**Files:**
- Create: `supabase/migrations/0071_sf_fluxo_op.sql`

**Interfaces:**
- Produces: `sf_fluxo_op(p_pmo text, p_op text)` → linhas `(posto text, wip int, registros int, aprovadas int, reprovadas int, retestes int)`. Uma linha por posto que tem registros e/ou WIP (postos do fluxo sem nenhum registro **não** vêm — a tela preenche com 0). O bucket `'Manutenção'` aparece quando há reprovas.

- [ ] **Step 1: Escrever a migração**

Criar `supabase/migrations/0071_sf_fluxo_op.sql`:
```sql
-- =============================================================
-- Fluxo da OP (estilo n8n): agregados por posto para o canvas.
-- WIP = posição atual da peça (último bipe; reprovado → Manutenção).
-- aprovadas/reprovadas/registros/retestes = TODOS os registros do posto.
-- retestes = SNs distintos com ≥2 registros no posto (reteste).
-- =============================================================
create or replace function public.sf_fluxo_op(p_pmo text, p_op text)
returns table (
  posto      text,
  wip        int,
  registros  int,
  aprovadas  int,
  reprovadas int,
  retestes   int
)
language plpgsql
stable
security definer
set search_path = public
as $$
begin
  if not tem_permissao('visualizar') then
    raise exception 'SEM_PERMISSAO';
  end if;

  return query
  with regs as (
    select numero_serie_norm, posto, status, data_hora, created_at
    from sf_registros
    where pmo = p_pmo and op = p_op and numero_serie_norm <> ''
  ),
  ult as ( -- último registro por SN (desempata por created_at)
    select distinct on (numero_serie_norm) numero_serie_norm, posto, status
    from regs
    order by numero_serie_norm, data_hora desc, created_at desc
  ),
  wip_t as ( -- reprovado → Manutenção; senão → posto do último registro
    select case when lower(status) = 'reprovado' then 'Manutenção' else posto end as posto,
           count(*)::int as wip
    from ult
    group by 1
  ),
  por_sn_posto as ( -- nº de registros de cada SN em cada posto (p/ retestes)
    select posto, numero_serie_norm, count(*) as vezes
    from regs
    group by posto, numero_serie_norm
  ),
  agg as (
    select posto,
           count(*) filter (where lower(status) = 'aprovado')::int  as aprovadas,
           count(*) filter (where lower(status) = 'reprovado')::int as reprovadas,
           count(*)::int as registros
    from regs
    group by posto
  ),
  ret as (
    select posto, count(*) filter (where vezes >= 2)::int as retestes
    from por_sn_posto
    group by posto
  ),
  postos as (
    select posto from agg
    union
    select posto from wip_t
  )
  select p.posto,
         coalesce(w.wip, 0)        as wip,
         coalesce(a.registros, 0)  as registros,
         coalesce(a.aprovadas, 0)  as aprovadas,
         coalesce(a.reprovadas, 0) as reprovadas,
         coalesce(rt.retestes, 0)  as retestes
  from postos p
  left join wip_t w on w.posto = p.posto
  left join agg   a on a.posto = p.posto
  left join ret  rt on rt.posto = p.posto;
end;
$$;
```

- [ ] **Step 2: Aplicar no Dev**

Run:
```bash
export SUPABASE_GO_BINARY="$HOME/.local/share/supabase/supabase-go"
supabase db push
```
Expected: aplica `0071_sf_fluxo_op` sem erro (Dev linkado).

- [ ] **Step 3: Sanidade da RPC no Dev**

Run (SQL via psql/estúdio — escolher uma OP real que tenha registros):
```sql
select * from sf_fluxo_op('<PMO_REAL>', '<OP_REAL>') order by posto;
```
Expected: uma linha por posto com números plausíveis; se houver reprovas, uma linha `Manutenção` com `wip > 0`. Se a RPC não puder ser exercida por falta de dado/permissão no ambiente, registrar isso no report e seguir (validação real no smoke).

- [ ] **Step 4: Commit**
```bash
git add supabase/migrations/0071_sf_fluxo_op.sql
git commit -m "feat(shopfloor): RPC sf_fluxo_op — agregados por posto p/ o Fluxo da OP (WIP=último bipe, reprova→Manutenção)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Domain — builder puro de nós/arestas (`fluxo-op.ts`)

**Files:**
- Create: `src/modules/shopfloor/domain/fluxo-op.ts`
- Test: `src/modules/shopfloor/domain/__tests__/fluxo-op.test.ts`

**Interfaces:**
- Consumes: nada externo (puro).
- Produces:
  - `interface FluxoAgregado { posto: string; wip: number; registros: number; aprovadas: number; reprovadas: number; retestes: number }`
  - `interface FluxoNodeData extends FluxoAgregado { ehManutencao: boolean; temStatus: boolean }`
  - `interface FluxoNodePos { id: string; x: number; y: number; data: FluxoNodeData }`
  - `interface FluxoEdge { id: string; source: string; target: string; tipo: 'fluxo' | 'reprova' }`
  - `const MANUTENCAO = 'Manutenção'`
  - `function construirFluxo(postosOrdenados: string[], agregados: FluxoAgregado[], temStatus: (posto: string) => boolean): { nodes: FluxoNodePos[]; edges: FluxoEdge[] }`

Regras do builder:
- Casar posto/agregado **case-insensitive**.
- Nós da cadeia: para cada posto em `postosOrdenados`, `id = posto`, `x = i * 220`, `y = 0`, `data` = agregado (default tudo 0 se não houver) + `temStatus(posto)`, `ehManutencao = false`.
- Nó Manutenção: **sempre** presente, `id = MANUTENCAO`, `x = ((postosOrdenados.length - 1) * 220) / 2` (centralizado; 0 se vazio), `y = 180`, `data` = agregado de `'Manutenção'` (default 0), `temStatus = false`, `ehManutencao = true`.
- Arestas de fluxo: `postos[i] → postos[i+1]`, `id = "f:"+source+"->"+target`, `tipo: 'fluxo'`.
- Arestas de reprova: para cada posto da cadeia com `reprovadas > 0`, `posto → MANUTENCAO`, `id = "r:"+posto`, `tipo: 'reprova'`.

- [ ] **Step 1: Escrever os testes (falhando)**

Criar `src/modules/shopfloor/domain/__tests__/fluxo-op.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { construirFluxo, MANUTENCAO, type FluxoAgregado } from '../fluxo-op'

const zero = (posto: string): FluxoAgregado => ({ posto, wip: 0, registros: 0, aprovadas: 0, reprovadas: 0, retestes: 0 })

describe('construirFluxo', () => {
  it('cria um nó por posto na ordem + nó de Manutenção sempre', () => {
    const { nodes } = construirFluxo(['Solda', 'Teste'], [], () => false)
    const ids = nodes.map((n) => n.id)
    expect(ids).toEqual(['Solda', 'Teste', MANUTENCAO])
    expect(nodes[0].x).toBe(0)
    expect(nodes[1].x).toBe(220)
    expect(nodes.find((n) => n.id === MANUTENCAO)!.ehManutencao ?? nodes.find((n) => n.id === MANUTENCAO)!.data.ehManutencao).toBe(true)
    expect(nodes.find((n) => n.id === MANUTENCAO)!.y).toBe(180)
  })

  it('encaixa os agregados no nó certo (case-insensitive) e aplica temStatus', () => {
    const agg: FluxoAgregado[] = [{ posto: 'teste', wip: 3, registros: 10, aprovadas: 7, reprovadas: 3, retestes: 2 }]
    const { nodes } = construirFluxo(['Solda', 'Teste'], agg, (p) => p === 'Teste')
    const teste = nodes.find((n) => n.id === 'Teste')!
    expect(teste.data.wip).toBe(3)
    expect(teste.data.aprovadas).toBe(7)
    expect(teste.data.temStatus).toBe(true)
    const solda = nodes.find((n) => n.id === 'Solda')!
    expect(solda.data.registros).toBe(0)
    expect(solda.data.temStatus).toBe(false)
  })

  it('leva o WIP de reprovas pro nó Manutenção', () => {
    const agg: FluxoAgregado[] = [{ ...zero(MANUTENCAO), wip: 4 }]
    const { nodes } = construirFluxo(['Teste'], agg, () => true)
    expect(nodes.find((n) => n.id === MANUTENCAO)!.data.wip).toBe(4)
  })

  it('liga a cadeia em sequência e cria aresta de reprova só onde reprovadas>0', () => {
    const agg: FluxoAgregado[] = [{ ...zero('Teste'), reprovadas: 2 }]
    const { edges } = construirFluxo(['Solda', 'Teste'], agg, () => true)
    expect(edges).toContainEqual({ id: 'f:Solda->Teste', source: 'Solda', target: 'Teste', tipo: 'fluxo' })
    expect(edges).toContainEqual({ id: 'r:Teste', source: 'Teste', target: MANUTENCAO, tipo: 'reprova' })
    expect(edges.some((e) => e.id === 'r:Solda')).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/modules/shopfloor/domain/__tests__/fluxo-op.test.ts`
Expected: FAIL (módulo `../fluxo-op` não existe).

- [ ] **Step 3: Implementar `fluxo-op.ts`**

Criar `src/modules/shopfloor/domain/fluxo-op.ts`:
```ts
export const MANUTENCAO = 'Manutenção'
const ESPACO_X = 220
const Y_MANUTENCAO = 180

export interface FluxoAgregado {
  posto: string
  wip: number
  registros: number
  aprovadas: number
  reprovadas: number
  retestes: number
}

export interface FluxoNodeData extends FluxoAgregado {
  ehManutencao: boolean
  temStatus: boolean
}

export interface FluxoNodePos {
  id: string
  x: number
  y: number
  data: FluxoNodeData
}

export interface FluxoEdge {
  id: string
  source: string
  target: string
  tipo: 'fluxo' | 'reprova'
}

function acharAgg(agregados: FluxoAgregado[], posto: string): FluxoAgregado | undefined {
  const alvo = posto.toLowerCase()
  return agregados.find((a) => a.posto.toLowerCase() === alvo)
}

function dados(posto: string, agregados: FluxoAgregado[], temStatus: boolean, ehManutencao: boolean): FluxoNodeData {
  const a = acharAgg(agregados, posto)
  return {
    posto,
    wip: a?.wip ?? 0,
    registros: a?.registros ?? 0,
    aprovadas: a?.aprovadas ?? 0,
    reprovadas: a?.reprovadas ?? 0,
    retestes: a?.retestes ?? 0,
    temStatus,
    ehManutencao,
  }
}

/** Monta nós/arestas do canvas a partir da ordem do fluxo + agregados da RPC. Puro. */
export function construirFluxo(
  postosOrdenados: string[],
  agregados: FluxoAgregado[],
  temStatus: (posto: string) => boolean,
): { nodes: FluxoNodePos[]; edges: FluxoEdge[] } {
  const nodes: FluxoNodePos[] = postosOrdenados.map((posto, i) => ({
    id: posto,
    x: i * ESPACO_X,
    y: 0,
    data: dados(posto, agregados, temStatus(posto), false),
  }))

  const xManut = postosOrdenados.length > 0 ? ((postosOrdenados.length - 1) * ESPACO_X) / 2 : 0
  nodes.push({
    id: MANUTENCAO,
    x: xManut,
    y: Y_MANUTENCAO,
    data: dados(MANUTENCAO, agregados, false, true),
  })

  const edges: FluxoEdge[] = []
  for (let i = 0; i < postosOrdenados.length - 1; i++) {
    const source = postosOrdenados[i]!
    const target = postosOrdenados[i + 1]!
    edges.push({ id: `f:${source}->${target}`, source, target, tipo: 'fluxo' })
  }
  for (const posto of postosOrdenados) {
    if ((acharAgg(agregados, posto)?.reprovadas ?? 0) > 0) {
      edges.push({ id: `r:${posto}`, source: posto, target: MANUTENCAO, tipo: 'reprova' })
    }
  }
  return { nodes, edges }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/modules/shopfloor/domain/__tests__/fluxo-op.test.ts`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**
```bash
git add src/modules/shopfloor/domain/fluxo-op.ts src/modules/shopfloor/domain/__tests__/fluxo-op.test.ts
git commit -m "feat(shopfloor): builder puro do Fluxo da OP (nós/arestas + Manutenção como ramo)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Repositório + action (`fluxo-repository.ts`, `fluxo-actions.ts`)

**Files:**
- Create: `src/modules/shopfloor/infra/fluxo-repository.ts`
- Create: `src/modules/shopfloor/application/fluxo-actions.ts`

**Interfaces:**
- Consumes: `sf_fluxo_op` (Task 1); `construirFluxo`, tipos (Task 2); `createServerSupabase` de `@/shared/lib/supabase/server`; `mapaPostoPerfil` de `@/modules/shopfloor/infra/postos-repository`; `getSessao`/`podeNoModulo` (padrão das outras actions).
- Produces (repository):
  - `interface OpItem { pmo: string; op: string; cliente: string; descricao: string }`
  - `interface SnDoPosto { sn: string; status: string; vezes: number }`
  - `listarOrdens(): Promise<OpItem[]>`
  - `carregarFluxoOp(pmo: string, op: string): Promise<{ postos: string[]; agregados: FluxoAgregado[]; temStatus: Record<string, boolean> }>`
  - `carregarSnsDoPosto(pmo: string, op: string, posto: string): Promise<SnDoPosto[]>`
- Produces (action):
  - `carregarFluxo(pmo, op): Promise<{ ok: true; nodes: FluxoNodePos[]; edges: FluxoEdge[] } | { ok: false; erro: string }>`
  - `snsDoPosto(pmo, op, posto): Promise<{ ok: true; sns: SnDoPosto[] } | { ok: false; erro: string }>`

- [ ] **Step 1: Escrever `fluxo-repository.ts`**
```ts
import 'server-only'
import { createServerSupabase } from '@/shared/lib/supabase/server'
import { mapaPostoPerfil } from './postos-repository'
import type { FluxoAgregado } from '../domain/fluxo-op'

export interface OpItem { pmo: string; op: string; cliente: string; descricao: string }
export interface SnDoPosto { sn: string; status: string; vezes: number }

/** Todas as OPs (pra escolher no seletor do Fluxo). */
export async function listarOrdens(): Promise<OpItem[]> {
  const supabase = await createServerSupabase()
  const { data, error } = await supabase
    .from('sf_ordens')
    .select('pmo,op,cliente,descricao')
    .order('pmo')
    .order('op')
  if (error) throw error
  return (data ?? []) as OpItem[]
}

/** Postos ordenados do fluxo + agregados (RPC) + mapa temStatus por posto. */
export async function carregarFluxoOp(
  pmo: string,
  op: string,
): Promise<{ postos: string[]; agregados: FluxoAgregado[]; temStatus: Record<string, boolean> }> {
  const supabase = await createServerSupabase()

  const { data: ordem, error: e1 } = await supabase
    .from('sf_ordens')
    .select('sf_ordem_postos(posto,ordem)')
    .eq('pmo', pmo)
    .eq('op', op)
    .maybeSingle()
  if (e1) throw e1
  const lista = ((ordem?.sf_ordem_postos ?? []) as { posto: string; ordem: number }[])
  const postos = [...lista].sort((a, b) => a.ordem - b.ordem).map((p) => p.posto)

  const { data: agg, error: e2 } = await supabase.rpc('sf_fluxo_op', { p_pmo: pmo, p_op: op })
  if (e2) throw e2
  const agregados = (agg ?? []) as FluxoAgregado[]

  const perfis = await mapaPostoPerfil()
  const temStatus: Record<string, boolean> = {}
  for (const p of postos) temStatus[p] = perfis[p]?.temStatus ?? false

  return { postos, agregados, temStatus }
}

/** SNs registrados num posto da OP (lazy, ao abrir o nó). Paginado. */
export async function carregarSnsDoPosto(pmo: string, op: string, posto: string): Promise<SnDoPosto[]> {
  const supabase = await createServerSupabase()
  const PAGINA = 1000
  const linhas: { numero_serie: string; numero_serie_norm: string; status: string; data_hora: string }[] = []
  for (let i = 0; ; i++) {
    const { data, error } = await supabase
      .from('sf_registros')
      .select('numero_serie,numero_serie_norm,status,data_hora')
      .eq('pmo', pmo)
      .eq('op', op)
      .ilike('posto', posto)
      .order('data_hora', { ascending: false })
      .range(i * PAGINA, i * PAGINA + PAGINA - 1)
    if (error) throw error
    const lote = (data ?? []) as typeof linhas
    linhas.push(...lote)
    if (lote.length < PAGINA) break
  }
  // Agrupa por SN (normalizado): status = o mais recente (linhas já vêm desc por data_hora), vezes = nº de registros.
  const porSn = new Map<string, SnDoPosto>()
  for (const l of linhas) {
    const chave = l.numero_serie_norm || l.numero_serie
    const atual = porSn.get(chave)
    if (atual) atual.vezes += 1
    else porSn.set(chave, { sn: l.numero_serie, status: l.status, vezes: 1 })
  }
  return [...porSn.values()].sort((a, b) => a.sn.localeCompare(b.sn))
}
```

- [ ] **Step 2: Escrever `fluxo-actions.ts`**
```ts
'use server'

import { getSessao } from '@/modules/auth/application/get-sessao'
import { podeNoModulo } from '@/modules/auth/domain/perfil'
import { construirFluxo, type FluxoNodePos, type FluxoEdge } from '@/modules/shopfloor/domain/fluxo-op'
import { carregarFluxoOp, carregarSnsDoPosto, type SnDoPosto } from '@/modules/shopfloor/infra/fluxo-repository'

const SEM_PERMISSAO = 'Você não tem permissão para esta ação.'

export async function carregarFluxo(
  pmo: string,
  op: string,
): Promise<{ ok: true; nodes: FluxoNodePos[]; edges: FluxoEdge[] } | { ok: false; erro: string }> {
  const sessao = await getSessao()
  if (!sessao || !podeNoModulo(sessao.perfil, 'shopfloor', 'visualizar')) return { ok: false, erro: SEM_PERMISSAO }
  try {
    const { postos, agregados, temStatus } = await carregarFluxoOp(pmo.trim(), op.trim())
    const { nodes, edges } = construirFluxo(postos, agregados, (p) => temStatus[p] ?? false)
    return { ok: true, nodes, edges }
  } catch {
    return { ok: false, erro: 'Não foi possível carregar o fluxo da OP.' }
  }
}

export async function snsDoPosto(
  pmo: string,
  op: string,
  posto: string,
): Promise<{ ok: true; sns: SnDoPosto[] } | { ok: false; erro: string }> {
  const sessao = await getSessao()
  if (!sessao || !podeNoModulo(sessao.perfil, 'shopfloor', 'visualizar')) return { ok: false, erro: SEM_PERMISSAO }
  try {
    return { ok: true, sns: await carregarSnsDoPosto(pmo.trim(), op.trim(), posto.trim()) }
  } catch {
    return { ok: false, erro: 'Não foi possível carregar os Nº de Série do posto.' }
  }
}
```

- [ ] **Step 3: Build + lint + testes** → verdes.

Run: `npm run build && npm run lint && npx vitest run`
Expected: sem erro. (Se `podeNoModulo`/`getSessao` tiverem outro caminho de import, alinhar com `embalagem-actions.ts` — que usa exatamente esses.)

- [ ] **Step 4: Commit**
```bash
git add src/modules/shopfloor/infra/fluxo-repository.ts src/modules/shopfloor/application/fluxo-actions.ts
git commit -m "feat(shopfloor): repositório + actions do Fluxo da OP (RPC + ordem do fluxo + SNs por posto lazy)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Dependência + aba + tela base (seletor de OP + canvas)

**Files:**
- Modify: `package.json`, `package-lock.json` (via npm install)
- Modify: `src/app/(app)/shopfloor/analisar/layout.tsx`
- Create: `src/app/(app)/shopfloor/analisar/fluxo/page.tsx`
- Create: `src/app/(app)/shopfloor/analisar/fluxo/fluxo-form.tsx`

**Interfaces:**
- Consumes: `carregarFluxo` (Task 3); `listarOrdens`, `OpItem` (Task 3); `FluxoNodePos`, `FluxoEdge` (Task 2); `@xyflow/react`.
- Produces: rota `/shopfloor/analisar/fluxo` renderizando o canvas com nós default do React Flow (o nó custom vem na Task 5).

- [ ] **Step 1: Instalar a lib**

Run: `npm install @xyflow/react`
Expected: adiciona `@xyflow/react` em `dependencies`; lockfile atualizado.

- [ ] **Step 2: Acrescentar a aba "Fluxo"**

Em `src/app/(app)/shopfloor/analisar/layout.tsx`, no array `ABAS`, acrescentar **após 'Caixas'**:
```ts
  { rotulo: 'Fluxo', href: '/shopfloor/analisar/fluxo' },
```

- [ ] **Step 3: `page.tsx` (server)**

Criar `src/app/(app)/shopfloor/analisar/fluxo/page.tsx`:
```tsx
import { getSessao } from '@/modules/auth/application/get-sessao'
import { podeNoModulo } from '@/modules/auth/domain/perfil'
import { SemPermissao } from '@/shared/ui/sem-permissao'
import { listarOrdens } from '@/modules/shopfloor/infra/fluxo-repository'
import { FluxoForm } from './fluxo-form'

export default async function FluxoPage() {
  const sessao = await getSessao()
  if (!sessao || !podeNoModulo(sessao.perfil, 'shopfloor', 'visualizar')) {
    return <SemPermissao descricao="Você não tem permissão para ver o fluxo das OPs." />
  }
  const ops = await listarOrdens()
  return (
    <div className="flex flex-col gap-4">
      <div>
        <h2 className="text-lg font-semibold text-tinta">Fluxo da OP</h2>
        <p className="text-sm text-muted-foreground">Escolha a OP para ver o fluxo de postos e onde as peças estão.</p>
      </div>
      <FluxoForm ops={ops} />
    </div>
  )
}
```

- [ ] **Step 4: `fluxo-form.tsx` (client) — seletor + canvas base**

Criar `src/app/(app)/shopfloor/analisar/fluxo/fluxo-form.tsx`. Nesta task o canvas usa **nós default** do React Flow (só pra provar que renderiza/pan/zoom); o nó custom entra na Task 5. Mapear `FluxoNodePos`→`Node` e `FluxoEdge`→`Edge`:
```tsx
'use client'

import { useCallback, useState, useTransition } from 'react'
import { ReactFlow, Background, Controls, type Node, type Edge } from '@xyflow/react'
import '@xyflow/react/dist/style.css'
import { toast } from 'sonner'
import { Card, CardContent } from '@/components/ui/card'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { carregarFluxo } from '@/modules/shopfloor/application/fluxo-actions'
import type { OpItem } from '@/modules/shopfloor/infra/fluxo-repository'
import type { FluxoNodePos, FluxoEdge } from '@/modules/shopfloor/domain/fluxo-op'

function paraNodes(pos: FluxoNodePos[]): Node[] {
  return pos.map((n) => ({ id: n.id, position: { x: n.x, y: n.y }, data: { label: `${n.data.posto} · ${n.data.wip}` } }))
}
function paraEdges(es: FluxoEdge[]): Edge[] {
  return es.map((e) => ({
    id: e.id, source: e.source, target: e.target,
    animated: e.tipo === 'reprova',
    style: e.tipo === 'reprova' ? { strokeDasharray: '4 4' } : undefined,
  }))
}

export function FluxoForm({ ops }: { ops: OpItem[] }) {
  const [sel, setSel] = useState('')
  const [nodes, setNodes] = useState<Node[]>([])
  const [edges, setEdges] = useState<Edge[]>([])
  const [buscou, setBuscou] = useState(false)
  const [carregando, startCarregar] = useTransition()

  const escolher = useCallback((v: string) => {
    setSel(v)
    setBuscou(false)
    const [pmo, op] = v.split('||')
    if (!pmo || !op) return
    startCarregar(async () => {
      const r = await carregarFluxo(pmo, op)
      if (!r.ok) { toast.error(r.erro); return }
      setNodes(paraNodes(r.nodes))
      setEdges(paraEdges(r.edges))
      setBuscou(true)
    })
  }, [])

  return (
    <Card>
      <CardContent className="flex flex-col gap-4">
        <div className="flex flex-col gap-1.5 sm:max-w-md">
          <Label>OP</Label>
          <Select value={sel} onValueChange={(v) => escolher(v ?? '')}>
            <SelectTrigger><SelectValue placeholder="Selecione a OP" /></SelectTrigger>
            <SelectContent>
              {ops.map((o) => (
                <SelectItem key={`${o.pmo}||${o.op}`} value={`${o.pmo}||${o.op}`}>
                  {o.pmo}/{o.op}{o.cliente ? ` · ${o.cliente}` : ''}
                </SelectItem>
              ))}
            </SelectContent>
          </Select>
        </div>

        {carregando && <p className="text-sm text-muted-foreground">Carregando…</p>}
        {buscou && !carregando && nodes.length === 0 && (
          <p className="text-sm text-muted-foreground">Esta OP não tem postos no fluxo.</p>
        )}

        <div className="h-[70vh] w-full rounded-lg border border-border">
          <ReactFlow nodes={nodes} edges={edges} fitView nodesDraggable={false} nodesConnectable={false} elementsSelectable={false}>
            <Background />
            <Controls showInteractive={false} />
          </ReactFlow>
        </div>
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 5: Build + lint + testes** → verdes.

Run: `npm run build && npm run lint && npx vitest run`
Expected: sem erro. (Se o build reclamar de SSR do React Flow, garantir que `fluxo-form.tsx` é `'use client'` — já é. A página server só importa o form.)

- [ ] **Step 6: Commit**
```bash
git add "src/app/(app)/shopfloor/analisar/layout.tsx" "src/app/(app)/shopfloor/analisar/fluxo/page.tsx" "src/app/(app)/shopfloor/analisar/fluxo/fluxo-form.tsx" package.json package-lock.json
git commit -m "feat(shopfloor): aba Fluxo em Análise + canvas React Flow (seletor de OP, pan/zoom)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Nó custom (WIP + detalhe lazy) + arestas Manutenção

**Files:**
- Create: `src/app/(app)/shopfloor/analisar/fluxo/fluxo-node.tsx`
- Modify: `src/app/(app)/shopfloor/analisar/fluxo/fluxo-form.tsx`

**Interfaces:**
- Consumes: `FluxoNodePos`, `FluxoNodeData` (Task 2); `snsDoPosto`, `SnDoPosto` (Task 3); `@xyflow/react` (`Handle`, `Position`, `NodeProps`, `NodeTypes`); seletor da OP já vem do form.
- Produces: nó custom `FluxoNode` mostrando **nome + WIP** fechado e, ao **clicar**, o painel com aprovadas/reprovadas/retestes + lista de SNs (carregada via `snsDoPosto`).

- [ ] **Step 1: `fluxo-node.tsx` (nó custom)**

Criar `src/app/(app)/shopfloor/analisar/fluxo/fluxo-node.tsx`. O nó recebe, além de `FluxoNodeData`, um callback `onAbrir(posto)` e o estado de detalhe via `data` (o form injeta). Para manter o nó burro, ele só renderiza; o **fetch** dos SNs mora no form (Step 2), e o resultado é passado de volta pelo `data`.
```tsx
'use client'

import { memo } from 'react'
import { Handle, Position, type NodeProps } from '@xyflow/react'
import { Wrench } from 'lucide-react'
import type { FluxoNodeData } from '@/modules/shopfloor/domain/fluxo-op'
import type { SnDoPosto } from '@/modules/shopfloor/infra/fluxo-repository'

export interface FluxoNodePayload extends FluxoNodeData {
  aberto: boolean
  carregandoSns: boolean
  sns: SnDoPosto[]
  onAbrir: (posto: string) => void
}

function FluxoNodeBase({ data }: NodeProps) {
  const d = data as unknown as FluxoNodePayload
  return (
    <div className={`min-w-44 rounded-xl border bg-card shadow-sm ${d.ehManutencao ? 'border-amber-500' : 'border-border'}`}>
      {!d.ehManutencao && <Handle type="target" position={Position.Left} />}
      <button
        type="button"
        onClick={() => d.onAbrir(d.posto)}
        className="flex w-full items-center justify-between gap-3 px-3 py-2 text-left"
      >
        <span className="flex items-center gap-1.5 text-sm font-medium">
          {d.ehManutencao && <Wrench className="size-3.5 text-amber-600" />}
          {d.posto}
        </span>
        <span className={`rounded-md px-2 py-0.5 text-sm font-semibold ${d.wip > 0 ? 'bg-enterplak text-white' : 'bg-muted text-muted-foreground'}`}>
          {d.wip}
        </span>
      </button>

      {d.aberto && (
        <div className="border-t border-border px-3 py-2 text-xs">
          {d.temStatus ? (
            <div className="mb-2 flex gap-3">
              <span className="text-green-700">Aprov.: {d.aprovadas}</span>
              <span className="text-red-600">Reprov.: {d.reprovadas}</span>
              <span className="text-muted-foreground">Retestes: {d.retestes}</span>
            </div>
          ) : (
            <div className="mb-2 text-muted-foreground">Registradas: {d.registros}</div>
          )}
          <p className="mb-1 font-medium text-muted-foreground">Nº de Série ({d.sns.length})</p>
          {d.carregandoSns ? (
            <p className="text-muted-foreground">Carregando…</p>
          ) : (
            <ul className="flex max-h-40 flex-col gap-0.5 overflow-y-auto">
              {d.sns.length === 0 && <li className="text-muted-foreground">—</li>}
              {d.sns.map((s, i) => (
                <li key={`${s.sn}-${i}`} className="flex justify-between gap-2 font-mono">
                  <span>{s.sn}</span>
                  <span className="text-muted-foreground">{s.status || '—'}{s.vezes > 1 ? ` ×${s.vezes}` : ''}</span>
                </li>
              ))}
            </ul>
          )}
        </div>
      )}
      <Handle type="source" position={Position.Right} />
    </div>
  )
}

export const FluxoNode = memo(FluxoNodeBase)
```

- [ ] **Step 2: Ligar o nó custom no form (fetch lazy + injeção no `data`)**

Em `fluxo-form.tsx`:
1. Trocar imports: acrescentar `useMemo`, `useRef`; importar `snsDoPosto`; importar `FluxoNode`, `type FluxoNodePayload`; importar `type NodeTypes` de `@xyflow/react`; importar `FluxoNodePos` e `FluxoNodeData` (já tem `FluxoNodePos`).
2. Guardar os nós do domínio (`FluxoNodePos[]`) em estado próprio `dom`, e o estado de detalhe: `aberto: string | null`, `sns: SnDoPosto[]`, `carregandoSns`. Guardar `pmo/op` escolhidos (`ref` ou estado) pra o fetch lazy.
3. `nodeTypes` estável: `const nodeTypes = useMemo<NodeTypes>(() => ({ fluxo: FluxoNode }), [])`.
4. Ao carregar a OP: guardar `r.nodes` em `dom`, `pmo/op` no ref, resetar detalhe.
5. `onAbrir(posto)`: se já aberto no mesmo posto → fecha (`aberto=null`); senão `aberto=posto`, `carregandoSns=true`, chama `snsDoPosto(pmo, op, posto)` (do ref), guarda em `sns`.
6. `nodes` do React Flow via `useMemo` sobre `[dom, aberto, sns, carregandoSns]`: cada node `{ id, type: 'fluxo', position, data: { ...domNode.data, aberto: aberto===id, carregandoSns: aberto===id && carregandoSns, sns: aberto===id ? sns : [], onAbrir } }`.
7. Passar `nodeTypes={nodeTypes}` ao `<ReactFlow>`. Manter `fitView`, `nodesDraggable={false}`.

Código do corpo do componente (substitui o núcleo do form; mantém o `<Select>` e o wrapper do Step 4 da Task 4):
```tsx
  const [dom, setDom] = useState<FluxoNodePos[]>([])
  const [edges, setEdges] = useState<Edge[]>([])
  const [aberto, setAberto] = useState<string | null>(null)
  const [sns, setSns] = useState<SnDoPosto[]>([])
  const [buscou, setBuscou] = useState(false)
  const [carregando, startCarregar] = useTransition()
  const [carregandoSns, startSns] = useTransition()
  const ctx = useRef<{ pmo: string; op: string }>({ pmo: '', op: '' })

  const nodeTypes = useMemo<NodeTypes>(() => ({ fluxo: FluxoNode }), [])

  const onAbrir = useCallback((posto: string) => {
    setAberto((a) => (a === posto ? null : posto))
    setSns([])
    if (aberto === posto) return
    const { pmo, op } = ctx.current
    startSns(async () => {
      const r = await snsDoPosto(pmo, op, posto)
      if (r.ok) setSns(r.sns)
      else toast.error(r.erro)
    })
  }, [aberto])

  const escolher = useCallback((v: string) => {
    setSel(v); setBuscou(false); setAberto(null); setSns([])
    const [pmo, op] = v.split('||')
    if (!pmo || !op) return
    ctx.current = { pmo, op }
    startCarregar(async () => {
      const r = await carregarFluxo(pmo, op)
      if (!r.ok) { toast.error(r.erro); return }
      setDom(r.nodes)
      setEdges(paraEdges(r.edges))
      setBuscou(true)
    })
  }, [])

  const nodes = useMemo<Node[]>(() => dom.map((n) => ({
    id: n.id,
    type: 'fluxo',
    position: { x: n.x, y: n.y },
    data: {
      ...n.data,
      aberto: aberto === n.id,
      carregandoSns: aberto === n.id && carregandoSns,
      sns: aberto === n.id ? sns : [],
      onAbrir,
    } satisfies FluxoNodePayload,
  })), [dom, aberto, sns, carregandoSns, onAbrir])
```
(Remover o `paraNodes` antigo e o estado `nodes`/`setNodes` da Task 4; `sel` continua `useState('')`.)

- [ ] **Step 3: Build + lint + testes** → verdes.

Run: `npm run build && npm run lint && npx vitest run`
Expected: sem erro. `satisfies FluxoNodePayload` casa o `data` com o payload do nó.

- [ ] **Step 4: Commit**
```bash
git add "src/app/(app)/shopfloor/analisar/fluxo/fluxo-node.tsx" "src/app/(app)/shopfloor/analisar/fluxo/fluxo-form.tsx"
git commit -m "feat(shopfloor): nó custom do Fluxo (WIP + detalhe lazy: aprov/reprov/retestes/SNs) + Manutenção como ramo

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Smoke (manual, ao fim da feature)
1. **Aba Fluxo:** aparece em Análise; abre sem erro.
2. **Seletor:** escolher uma OP com registros → canvas mostra os postos na ordem do fluxo, ligados por setas; **Manutenção** abaixo.
3. **WIP:** os números nos nós fechados batem com "onde as peças estão" (soma dos WIP dos postos + Manutenção = peças já bipadas da OP). Peça reprovada por último aparece contada em Manutenção.
4. **Detalhe:** clicar num nó de teste/inspeção → aprovadas/reprovadas/retestes + lista de SNs; clicar num posto sem status (embalagem/integração/burn-in) → "Registradas" + SNs. Fechar clicando de novo.
5. **Reprova:** postos que reprovaram têm aresta tracejada até Manutenção.
6. **Canvas:** pan, zoom e "encaixar na tela" (Controls) funcionam.

## Self-Review
- **Cobertura da spec:** aba/seletor → T4; RPC/agregados → T1; builder/Manutenção-ramo → T2; repositório+action+SNs lazy → T3; nó custom (WIP fechado, detalhe aberto) → T5. ✔
- **WIP = último bipe, reprova→Manutenção:** RPC (T1) + testado no builder o roteamento p/ Manutenção (T2). ✔
- **Sem status usa "Registradas":** `temStatus` no builder (T2) + rótulo no nó (T5). ✔
- **Tipos consistentes:** `FluxoAgregado`/`FluxoNodePos`/`FluxoEdge`/`FluxoNodeData` (T2) usados igual em T3/T4/T5; `SnDoPosto` (T3) usado em T5. ✔
- **Migração Dev-first (0071), só leitura, olhinho intacto.** ✔
