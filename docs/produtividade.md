# Produtividade — registro por sessão

> Registro leve pra acompanhar a evolução. Uma linha por sessão de trabalho.
> Formato ajustável — se quiser medir de outro jeito, é só mudar. Medição iniciada em **2026-07-29**.

| Data | Entregue | Commits | Notas |
|---|---|---|---|
| 2026-07-30 | **Onda 2 — Cadastrar Posto + Perfis de Posto (fundacional):** comportamento do posto passa a ser por **perfil** (não por nome). Migração `0062` (`sf_posto_perfis` 9 seeds + `sf_postos.perfil` backfill) + `0063` (policy de escrita `sf_postos_admin`). Novo `domain/perfil-posto.ts`; refatorados **6 arquivos** (lancar/integracao/grade/dashboard/lancamento-form) nome→perfil; **removido** todo o nome-based; nova **tela Cadastrar Posto** (nome+ordem+perfil; editar/excluir só se livre). **8 tasks** subagentes (review por task, 1 Critical corrigido: `burnin.gate`; 1 Critical corrigido: RLS-escrita `sf_postos`) + review final opus **READY**. RPCs intactos; **paridade total** dos 9 postos. | (em aberto) | 301 testes. `0062`+`0063` no Dev. 3 Minor não-bloqueantes (Manutenção inerte · texto SPI · Análise ainda name-based na exibição → Fase 2). Backlog: Fase 2 dos perfis. |
| 2026-07-30 | **Batch merge pro Prod** (0058–0060, PR #3, deploy, smoke ok — 7 melhorias no ar, dark launch) · **Scroll no topo da Manutenção** (reusa `ScrollHorizontalTopo`, movido pra `shared/ui`) · **Onda 1 item 3 — Reparo (+campos):** defeito **relatado** no cabeçalho + seção **defeitos constatados** (do catálogo, ≥1 obrigatório, contam como defeito da peça). Migração `0061` (coluna `reparo_constatado` + RPC redefinido) **no Dev**. Reparo via 3 tasks subagentes + review opus **READY**. | (em aberto) | 306 testes. Branch nova `feat/shopfloor-ondas` (da main). ⚠️ senha do Prod vazou num paste ontem → resetar. Backlog novo: **feedbacks/pop-ups maiores e mais claros** (sucesso/falha). Pendências: smoke do reparo + merge do lote quando fechar. |
| **2026-07-29 · FECHAMENTO DO DIA** | **2 features da Onda 1 entregues e aprovadas no smoke** (Cadastro de Defeitos + Tempo mínimo de Burn-in) + refinos. Cadência completa (brainstorm→mockup→spec→plano→subagentes com review por task→review final opus→smoke) em ambas. Branch `feat/shopfloor-pos-prod` fecha o dia **64 commits à frente da `main`**, com **7 melhorias** acumuladas pro batch. Migração `0060` aplicada no **Dev**; **Prod/main intactas** (merge adiado). | **32** (18 feat · 3 fix · 11 docs) | 310 testes. Medição: ~11 subagentes-implementer + ~10 reviews no dia, todos os reviews finais **READY**. Próximo: batch merge (0058+0059+0060 → main → deploy → smoke) + Onda 1 item 3 (Registrar reparo). |
| 2026-07-29 | **Onda 1 item 2 — Tempo mínimo de Burn-in:** campo "Tempo mín. Burn-in" por OP, **na linha do posto Burn-in** (só aparece com Burn-in no fluxo); na **saída** do Burn-in, **aviso/confirmação** ("faltavam X; registrar mesmo assim?") se sair antes — **só avisa, não trava**. Migração `0060` (`sf_ordens.tempo_min_burnin`, no **Dev**). Brainstorm→spec→plano→**6 tasks** + fix Important fail-open + review opus **READY**. **Refinos (feedback de smoke):** campo movido pra linha do Burn-in; máscara **filtro** (digita o `:`; testou auto-`:` e descartou por "pulo"); **default 06:00** em OP nova. RPC `sf_burnin` intacto. **Smoke aprovado.** | 6+3+refinos | 310 testes (domínio hh:mm↔min + filtro). Minor follow-up: botão não desabilita durante o await do pré-check (RPC já barra duplo-envio). |
| 2026-07-29 | **Onda 1 da reunião — Cadastro de Defeitos:** tela admin em Configurações › **Ajustes ShopFloor › Defeitos** (novo accordion) para listar/buscar/cadastrar/excluir o catálogo `sf_defeitos`. Brainstorm→mockup→spec→plano→**5 tasks** por subagentes (implementer+review cada) + review final opus **READY TO MERGE**. Sem migração (tabela+RLS já existiam); guard em 3 camadas (página `shopfloor.administrar` + action + RLS); código normalizado (trim+MAIÚSCULAS), duplicado→erro amigável; excluir não afeta histórico. | 5+2 | 289 testes (+6). Reviews todos limpos; 2 Minor não-bloqueantes (excluir sem checar existência; `created_at` via default — confirmado no 0028). **Smoke aprovado.** |
| 2026-07-29 | **Refino + rework dos Padrões de Fluxo:** remoção dos chips + botão "Excluir padrão"; fix do nome no dropdown; **rework** (brainstorm→spec→plano→2 tasks): auto-preencher **Cliente+Descrição pela PMO** (OP mais recente), padrão **"Associar à PMO"** escolhida no lugar da descrição, dropdown só o nome, **fix do bug do "cache"** (form reseta ao reabrir) + fix do banner de erro grudento. | 8 | Início da medição. Rework via subagentes (reviews limpos + opus Ready-to-merge). **Batch** na `feat/shopfloor-pos-prod` agora com **5 melhorias** (Faixa SN · Registros · Reestruturação · Padrões · rework), aguardando smoke + batch merge. Dev com 0058+0059. |
